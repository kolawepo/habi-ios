# Xcode project — generated by `xcodegen generate` from project.yml.
# Source of truth is project.yml; run `xcodegen generate` after cloning.
*.xcodeproj/

.DS_Store
build/
DerivedData/

# Swift Package Manager
.build/
.swiftpm/

# Firebase — contains real API keys, never commit
GoogleService-Info.plist

# Secrets / env
*.env
.env.*
